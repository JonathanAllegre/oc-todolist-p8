default:
    suites:
        default:
            contexts:
                - FeatureContext:
                    kernel: '@kernel'
                - WebContext

    extensions:
        Behat\Symfony2Extension:
            kernel:
                bootstrap: features/bootstrap/bootstrap.php
                class: App\Kernel
        Behat\MinkExtension:
            base_url: http://172.25.0.5:8080
            goutte: ~
